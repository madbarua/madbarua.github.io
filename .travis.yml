language: node_js
node_js:
 - "node"

cache: npm

script: npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_API_KEY
  keep_history: true
  local_dir: dist
  target_branch: master
  on:
    branch: dev